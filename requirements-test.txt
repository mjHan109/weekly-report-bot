# Test dependencies for Phase 3 QA suite
# Install with: pip install -r requirements-test.txt

pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
httpx>=0.27.0          # AsyncClient / TestClient support
aiosqlite>=0.20.0      # in-memory async SQLite for test fixtures
